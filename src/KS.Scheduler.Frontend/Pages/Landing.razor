@page "/"
@inject NavigationManager NavManager
@layout Layout.EmptyLayout
@inject IJSRuntime JSRuntime

<PageTitle>KS Scheduler</PageTitle>

<div class="landing-container">

    <nav class="navbar-fixed">
        <div class="container d-flex justify-content-between align-items-center">
            <div class="logo fw-bold text-white fs-4">KS <span class="text-gradient">Scheduler</span></div>
            <button class="btn btn-outline-light rounded-pill px-4 btn-sm" @onclick='() => NavManager.NavigateTo("/login")'>
                Login
            </button>
        </div>
    </nav>

    <header class="hero-section">
        <div class="container text-center animate-up">
            <h1 class="hero-title text-white text-uppercase">
                Gerencie seu futebol<br>
                <span class="text-gradient">como um profissional.</span>
            </h1>
            <p class="hero-subtitle mx-auto">
                Acabe com a confusão no WhatsApp. Confirmação de presença, sorteio de times e pagamentos em um só lugar.
            </p>
            <button class="btn btn-cta mt-4" @onclick='() => NavManager.NavigateTo("/login")'>
                Começar Agora 
            </button>
        </div>
    </header>

    <section class="features-section">
        <div class="container">
            <div class="row mb-5 text-center">
                <div class="col-12">
                    <h2 class="fw-bold text-white mb-2">Funcionalidades da Ferramenta</h2>
                    <p class="text-gray mx-auto" style="max-width: 600px;">
                        Ferramenta desenvolvidas para quem organiza e para quem joga.
                        Simples, visual e direto ao ponto.
                    </p>
                </div>
            </div>

            <div class="row g-4">
                <div class="col-12 col-md-4">
                    <div class="feature-card border-blue">
                        <div class="icon-box text-cyan">📅</div>
                        <h3 class="text-white mt-3 h5 fw-bold">Agendamento Fácil</h3>
                        <p class="text-gray small">Crie a partida, defina o local e hora. Seus amigos recebem o link e confirmam na hora.</p>
                    </div>
                </div>

                <div class="col-12 col-md-4">
                    <div class="feature-card border-green">
                        <div class="icon-box text-green">👕</div>
                        <h3 class="text-white mt-3 h5 fw-bold">Organização de Times</h3>
                        <p class="text-gray small">Sistema de divisão de times equilibrados. Rapidez antes mesmo de começar a jogar.</p>
                    </div>
                </div>

                <div class="col-12 col-md-4">
                    <div class="feature-card border-gold">
                        <div class="icon-box text-gold">💰</div>
                        <h3 class="text-white mt-3 h5 fw-bold">Controle Financeiro</h3>
                        <p class="text-gray small">Saiba exatamente quem pagou e quem está devendo. O fim do calote na pelada.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="showcase-section">
        <div class="container text-center mb-5">
            <h2 class="fw-bold text-white">Veja na prática</h2>
            <p class="text-gray">Passe o mouse (ou toque) para ampliar os detalhes.</p>
        </div>

        <div class="phones-container">
            <div class="phone-mockup left">
                <div class="screen-placeholder">
                    <span>Lista de<br>Jogadores</span>
                </div>
            </div>

            <div class="phone-mockup center">
                <div class="screen-placeholder main">
                    <span>Confirmar<br>Presença</span>
                    <button class="btn btn-sm btn-light mt-2">✅ Jogar</button>
                </div>
            </div>

            <div class="phone-mockup right">
                <div class="screen-placeholder">
                    <span>Divisão de<br>Times</span>
                </div>
            </div>

            <div class="fade-overlay"></div>
        </div>
    </section>

    <footer class="text-center py-4 text-gray border-top border-secondary border-opacity-25 mt-0 bg-darker">
        <small>&copy; 2026 KS Scheduler. Todos os direitos reservados.</small>
    </footer>

</div>

<style>
    :root {
        --bg-dark: #0f172a;
        --bg-features: #1e293b;
        --primary: #10b981;
        --cyan: #06b6d4; 
        --gold: #f59e0b;
        --text-gray: #94a3b8;
    }

    body {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
        background-color: var(--bg-dark);
        margin: 0;
        overflow-x: hidden;
    }

    .landing-container {
        width: 100%;
        height: 100vh;
        overflow-y: auto;
        overflow-x: hidden;
        background-color: var(--bg-dark);
    }

    .navbar-fixed {
        position: fixed;
        top: 0;
        width: 100%;
        padding: 1rem 0;
        z-index: 1000;
        background: rgba(15, 23, 42, 0.8);
        backdrop-filter: blur(10px);
        border-bottom: 1px solid rgba(255,255,255,0.05);
    }

    .hero-section {
        padding-top: 160px;
        padding-bottom: 80px;
        background: radial-gradient(circle at top center, #1e293b 0%, #0f172a 70%);
    }

    .badge-pill {
        display: inline-flex;
        align-items: center;
        background: rgba(255, 255, 255, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.2);
        padding: 0.5rem 1rem;
        border-radius: 50px;
        font-size: 0.85rem;
        color: var(--primary);
        font-weight: 600;
    }

    .dot {
        width: 8px;
        height: 8px;
        background-color: var(--primary);
        border-radius: 50%;
        margin-right: 8px;
        box-shadow: 0 0 10px var(--primary);
    }

    .hero-title {
        font-weight: 900;
        font-size: 3.5rem;
        line-height: 1.1;
        letter-spacing: -1px;
        margin-bottom: 1.5rem;
    }

    .text-gradient {
        background: linear-gradient(135deg, #34d399 0%, #059669 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .hero-subtitle {
        color: var(--text-gray);
        font-size: 1.1rem;
        max-width: 600px;
        line-height: 1.6;
    }

    .btn-cta {
        background: linear-gradient(135deg, var(--primary) 0%, #047857 100%);
        color: white;
        border: none;
        padding: 1rem 2.5rem;
        font-size: 1.1rem;
        font-weight: bold;
        border-radius: 50px;
        transition: transform 0.2s, box-shadow 0.2s;
        box-shadow: 0 10px 25px -5px rgba(16, 185, 129, 0.4);
    }

        .btn-cta:hover {
            transform: translateY(-2px);
            box-shadow: 0 15px 30px -5px rgba(16, 185, 129, 0.5);
            color: white;
        }

    .features-section {
        background-color: var(--bg-features);
        padding: 100px 0;
        border-top: 1px solid rgba(255,255,255,0.05);
        border-bottom: 1px solid rgba(255,255,255,0.05);
    }

    .feature-card {
        background: rgba(255, 255, 255, 0.03);
        padding: 2rem;
        border-radius: 16px;
        height: 100%;
        transition: transform 0.3s;
        border: 1px solid rgba(255, 255, 255, 0.05);
        position: relative;
        overflow: hidden;
    }

        .feature-card:hover {
            transform: translateY(-5px);
            background: rgba(255, 255, 255, 0.06);
        }

        .feature-card.border-blue {
            border-top: 4px solid var(--cyan);
        }

        .feature-card.border-green {
            border-top: 4px solid var(--primary);
        }

        .feature-card.border-gold {
            border-top: 4px solid var(--gold);
        }

        .feature-card.border-blue:hover {
            box-shadow: 0 0 30px rgba(6, 182, 212, 0.1);
        }

        .feature-card.border-green:hover {
            box-shadow: 0 0 30px rgba(16, 185, 129, 0.1);
        }

        .feature-card.border-gold:hover {
            box-shadow: 0 0 30px rgba(245, 158, 11, 0.1);
        }

    .icon-box {
        font-size: 2rem;
        margin-bottom: 1rem;
    }

    .text-cyan {
        color: var(--cyan);
    }

    .text-green {
        color: var(--primary);
    }

    .text-gold {
        color: var(--gold);
    }

    .text-gray {
        color: var(--text-gray);
    }

    .showcase-section {
        padding: 80px 0 0 0;
        background-color: var(--bg-dark);
        position: relative;
        overflow: hidden;
    }

    .phones-container {
        position: relative;
        height: 500px;
        display: flex;
        justify-content: center;
        align-items: flex-start;
        perspective: 1000px;
        margin-top: 2rem;
    }

    .phone-mockup {
        width: 260px;
        height: 520px;
        background: #000;
        border-radius: 40px;
        border: 8px solid #222;
        position: absolute;
        box-shadow: 0 20px 50px rgba(0,0,0,0.6);
        overflow: hidden;
        transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        cursor: pointer;
    }

    .screen-placeholder {
        width: 100%;
        height: 100%;
        background: #1e293b;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        color: white;
        font-weight: bold;
    }

        .screen-placeholder.main {
            background: linear-gradient(to bottom, #064e3b, #0f172a);
        }

    .phone-mockup.center {
        z-index: 10;
        transform: translateZ(0);
        border-color: #333;
    }

    .phone-mockup.left {
        z-index: 5;
        transform: translateX(-160px) scale(0.9) rotateY(15deg) translateY(20px);
        opacity: 0.6;
        filter: brightness(0.5);
    }

    .phone-mockup.right {
        z-index: 5;
        transform: translateX(160px) scale(0.9) rotateY(-15deg) translateY(20px);
        opacity: 0.6;
        filter: brightness(0.5);
    }

    .phone-mockup.left:hover {
        z-index: 20;
        transform: translateX(-160px) scale(1.05) rotateY(0deg) translateY(0);
        opacity: 1;
        filter: brightness(1);
        box-shadow: 0 30px 60px rgba(0,0,0,0.8);
    }

    .phone-mockup.right:hover {
        z-index: 20;
        transform: translateX(160px) scale(1.05) rotateY(0deg) translateY(0);
        opacity: 1;
        filter: brightness(1);
        box-shadow: 0 30px 60px rgba(0,0,0,0.8);
    }

    .phone-mockup.center:hover {
        transform: scale(1.02);
    }

    .fade-overlay {
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        height: 150px;
        background: linear-gradient(to top, var(--bg-dark) 20%, transparent 100%);
        z-index: 30;
        pointer-events: none;
    }

    @@media (max-width: 768px) {
        .hero-title {
            font-size: 2.5rem;
        }

        .hero-section {
            padding-top: 120px;
        }

        .phones-container {
            height: auto;
            flex-direction: column;
            align-items: center;
            padding-bottom: 50px;
            perspective: none;
        }

        .phone-mockup {
            position: relative;
            transform: none !important;
            opacity: 1 !important;
            filter: brightness(1) !important;
            margin-bottom: 2rem;
            width: 80%;
            max-width: 300px;
            left: auto !important;
            right: auto !important;
        }
    }
</style>